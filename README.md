# 🔌 Router Access Helper

Веб-приложение для быстрого доступа к админке роутеров. Хранит логины и пароли для различных моделей роутеров и модемов, позволяет быстро копировать учётные данные и открывать настройки роутера.

![PWA](https://img.shields.io/badge/PWA-enabled-4285F4?logo=pwa&logoColor=white)
![Mobile-first](https://img.shields.io/badge/Mobile-first-000000?logo=mobile&logoColor=white)
![No build](https://img.shields.io/badge/No%20build-required-00D9FF?logo=html5)

## ✨ Особенности

- 📱 **Mobile-first дизайн** — оптимизирован для мобильных устройств
- 🌐 **Многоязычность** — поддержка русского и английского языков
- 🌓 **Тёмная/светлая тема** — автоматическое определение и ручное переключение
- 💾 **Локальное хранение** — все данные хранятся в браузере (localStorage)
- 📋 **Быстрое копирование** — один клик для копирования логина/пароля
- 🔒 **Работает офлайн** — PWA приложение, можно установить на устройство
- 🎨 **Современный UI** — чистый и интуитивный интерфейс
- 🔍 **Проверка доступности** — ping роутера для проверки подключения
- 📊 **Инструменты** — проверка скорости интернета, калькулятор подсетей
- 💾 **Резервное копирование** — экспорт и импорт настроек роутеров
- 📡 **Статус подключения** — индикатор онлайн/офлайн состояния

## 🚀 Быстрый старт

### Локальный запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/yourusername/router-access-helper.git
   cd router-access-helper
   ```

2. Откройте `index.html` в браузере или используйте локальный сервер:
   ```bash
   # Python
   python -m http.server 8000
   
   # Node.js
   npx serve
   
   # PHP
   php -S localhost:8000
   ```

3. Откройте в браузере: `http://localhost:8000`

### Публикация на GitHub Pages

1. Перейдите в **Settings** → **Pages**
2. Выберите источник: **Deploy from a branch**
3. Выберите ветку: `main` (или `master`)
4. Выберите папку: `/` (root)
5. Нажмите **Save**
6. Через несколько минут сайт будет доступен по адресу:
   `https://yourusername.github.io/router-access-helper/`

> 💡 Все пути в коде относительные, поэтому сайт будет работать на любом поддомене GitHub Pages.

## 📁 Структура проекта

```
router-access-helper/
├── index.html              # Главная страница
├── app.js                  # Основная логика приложения
├── styles.css              # Стили и темы
├── site.webmanifest        # PWA манифест
├── favicon.svg             # SVG иконка
├── favicon.ico             # ICO иконка
├── favicon-*.png           # PNG иконки разных размеров
├── apple-touch-icon.png    # Иконка для iOS
├── android-chrome-*.png    # Иконки для Android
├── ogimage.png             # Open Graph изображение
├── generate_icons.html     # Генератор иконок
├── assets/
│   └── svg/                # SVG иконки брендов
├── data/
│   └── router-db.json      # База данных роутеров
└── i18n/
    ├── i18n.js             # Система локализации
    ├── ru.json             # Русский язык
    └── en.json             # Английский язык
```

## 🎨 Генерация иконок

Для создания всех необходимых иконок из SVG:

1. Откройте `generate_icons.html` в браузере
2. Дождитесь автоматической генерации (или нажмите "Сгенерировать все иконки")
3. Нажмите "Скачать все файлы"
4. Скопируйте скачанные файлы в корень проекта

Инструмент использует встроенные возможности браузера и работает без установки дополнительных программ.

## 📝 Использование

### Добавление своего роутера

1. Откройте меню **"Инструменты"** (кнопка с иконкой шестерёнки в шапке)
2. Перейдите на вкладку **"Мои роутеры"**
3. Заполните форму:
   - Бренд (например: Huawei)
   - Модель (например: HG8245H)
   - Группа (автоматически определяется или выберите вручную)
   - IP адрес (опционально)
   - Пары логин/пароль (можно добавить несколько)
4. Нажмите **"Сохранить"**

### Использование дефолтных данных

1. Выберите роутер из списка **"Подсказки логина/пароля"**
2. Нажмите на карточку роутера для выбора
3. Нажмите на кнопку копирования рядом с парой логин/пароль для копирования
4. Нажмите **"Открыть настройки"** для перехода в админку роутера
5. Используйте кнопку **"Проверить"** для проверки доступности роутера

### Инструменты

#### Проверка скорости интернета
- Откройте **"Инструменты"** → вкладка **"Проверка скорости"**
- Просмотрите статус подключения (онлайн/офлайн, внешний IP, местоположение)
- Нажмите на кнопку с логотипом Яндекс Интернетометра для перехода к проверке скорости

#### Калькулятор подсетей
- Откройте **"Инструменты"** → вкладка **"Калькулятор подсетей"**
- Введите IP адрес и маску подсети (например: `192.168.1.0` и `/24` или `255.255.255.0`)
- Нажмите **"Рассчитать"** для получения информации о сети

#### Резервное копирование
- В разделе **"Мои роутеры"** найдите секцию **"Резервное копирование"**
- **Экспорт**: нажмите "Экспортировать" для скачивания файла с настройками
- **Импорт**: нажмите "Импортировать" и выберите ранее сохранённый JSON файл

## 🔧 Технологии

- **HTML5** — семантическая разметка
- **CSS3** — переменные, Grid, Flexbox
- **Vanilla JavaScript** — без фреймворков
- **PWA** — Progressive Web App с Service Worker
- **LocalStorage** — локальное хранение данных
- **Clipboard API** — копирование в буфер обмена
- **Fetch API** — проверка доступности роутеров и статуса подключения

## 🌍 Локализация

Приложение поддерживает два языка:
- 🇷🇺 Русский (по умолчанию)
- 🇬🇧 English

Язык определяется автоматически по настройкам браузера. Можно переключить вручную через меню в шапке.

## 🎨 Темы

Поддерживаются две темы:
- 🌙 Тёмная (по умолчанию для тёмной системы)
- ☀️ Светлая (по умолчанию для светлой системы)

Тема определяется автоматически по системным настройкам. Можно переключить вручную через кнопку в шапке.

## 📊 База данных роутеров

База данных находится в `data/router-db.json`. Вы можете расширять её, добавляя новые модели и бренды.

### Формат записи:

```json
{
  "defaultsCommon": [
    { "login": "admin", "password": "admin" },
    { "login": "admin", "password": "password" }
  ],
  "models": [
    {
      "brand": "Huawei",
      "model": "HG8245H",
      "group": "huawei",
      "ip": "http://192.168.100.1",
      "thumbnail": "./assets/svg/router.svg",
      "order": 0,
      "defaults": [
        {
          "login": "root",
          "password": "admin",
          "note": "Дефолтные данные"
        }
      ]
    }
  ]
}
```

### Поля модели:
- `brand` — бренд роутера (обязательно)
- `model` — модель роутера (обязательно)
- `group` — группа роутера (`"huawei"`, `"zte"` или массив групп)
- `ip` — IP адрес для доступа к админке (опционально)
- `thumbnail` — путь к иконке роутера (опционально)
- `order` — порядок сортировки (опционально, по умолчанию 999)
- `defaults` — массив пар логин/пароль (обязательно)

## ⚠️ Ограничения

- Из HTTPS-страницы нельзя опросить адреса роутеров напрямую (CORS/Mixed Content)
- Сайт открывает ссылки на роутеры (например, `http://192.168.1.1`) в новых вкладках
- Проверка доступности (ping) работает только для локальных IP адресов через загрузку favicon
- Для внешних IP адресов проверка может быть ограничена политиками безопасности браузера

## 🛠️ Разработка

### Требования

- Современный браузер с поддержкой ES6+
- Локальный веб-сервер (для разработки, обязателен для Service Worker)

### Запуск для разработки

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/router-access-helper.git
cd router-access-helper

# Запустить локальный сервер
python -m http.server 8000
# или
npx serve
# или
php -S localhost:8000
```

Откройте `http://localhost:8000` в браузере.

> ⚠️ **Важно**: Service Worker работает только через HTTP(S), не через `file://` протокол.

### Структура кода

- **Комментарии** — на русском языке
- **Иконки** — SVG файлы в `assets/svg/` и inline SVG
- **Стили** — CSS переменные для тем, mobile-first подход
- **Логика** — модульная структура в `app.js`
- **Локализация** — JSON файлы в `i18n/`, автоматическое определение языка
- **PWA** — Service Worker в `sw.js`, манифест в `site.webmanifest`

### Основные модули

- **Темы** — автоматическое определение и переключение тёмной/светлой темы
- **Локализация** — система i18n с поддержкой русского и английского
- **Роутеры** — загрузка базы данных, отображение карточек, выбор роутера
- **Мои роутеры** — CRUD операции для пользовательских роутеров
- **Проверка доступности** — ping роутеров через загрузку favicon
- **Инструменты** — проверка скорости, калькулятор подсетей
- **Экспорт/Импорт** — резервное копирование настроек роутеров
- **Офлайн режим** — Service Worker для работы без интернета

## 📄 Лицензия

Этот проект распространяется свободно. Используйте по своему усмотрению.

## 🤝 Вклад в проект

Приветствуются любые улучшения! Вы можете:

1. Форкнуть репозиторий
2. Создать ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитить изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушить в ветку (`git push origin feature/AmazingFeature`)
5. Открыть Pull Request

## 🎯 Функциональность

### Основные возможности

- ✅ Просмотр базы данных роутеров с группировкой по брендам
- ✅ Добавление собственных роутеров с настройками
- ✅ Быстрое копирование логинов и паролей
- ✅ Открытие админки роутера в новой вкладке
- ✅ Проверка доступности роутера (ping)
- ✅ Индикатор статуса подключения (онлайн/офлайн)
- ✅ Экспорт и импорт настроек роутеров
- ✅ Проверка скорости интернета (Яндекс Интернетометр)
- ✅ Калькулятор подсетей
- ✅ Поддержка тёмной и светлой темы
- ✅ Многоязычный интерфейс (RU/EN)
- ✅ Работа в офлайн режиме (PWA)

### Группы роутеров

Роутеры автоматически группируются по брендам:
- **OLT Huawei** — роутеры Huawei (IP: `192.168.100.1`)
- **OLT ZTE** — роутеры ZTE (IP: `192.168.1.1`)
- **Свои группы** — пользовательские группы с произвольными названиями

## 📞 Поддержка

Если у вас есть вопросы или предложения, создайте [Issue](https://github.com/yourusername/router-access-helper/issues) в репозитории.

---

⭐ Если проект оказался полезным, поставьте звезду!
